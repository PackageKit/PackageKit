# Configuration for running PackageKit straight from the build tree.
# This file is generated by Meson and used by the test suite only.

[Daemon]

# Treat the fake root laid out in the build tree as "/", so backend modules
# are loaded from there and no state is written to the host system
RootDir=@ROOT_DIR@
