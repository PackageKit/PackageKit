Order of no particular importance:

* Add GetStatus and ::StatusChanged to get package data as string for UI
* Add yum details script
* Add other PolicyKit server bits
* Add policykit application UI for the session
* Add details into the application UI
* Only try to do actions when online (sortof done, use PkNetwork)
* Go to refresh and update at start of session only if network is up
* Write documentation for backend writers
* Write documentation for UI porters
* Add FindPackagesByGroup() and GetGroupList

